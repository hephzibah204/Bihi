export const letter_generator = { name: 'letter_generator', description: 'Generate admin letter', parameters: { topic: { type: 'string' } }, run: async (args: any, _context: any) => { const topic = String(args?.topic || 'School Update'); return { letter: `Subject: ${topic}\n\nDear Parent,\n\n${topic} details are attached.\n\nRegards.` }; } };
export const sms_generator = { name: 'sms_generator', description: 'Generate SMS text', parameters: { purpose: { type: 'string' } }, run: async (args: any, _context: any) => { const p = String(args?.purpose || 'Reminder'); return { sms: `${p}: Please take action.` }; } };
export const policy_summary = { name: 'policy_summary', description: 'Summarize policy text', parameters: { text: { type: 'string' } }, run: async (args: any, _context: any) => { const txt = String(args?.text || ''); const words = txt.split(/\s+/).filter(Boolean).length; return { summary: txt.slice(0, 240), words }; } };
