import React from 'react';
import Assignments from './Assignments';

// This component can be a wrapper for now, providing a specific entry point for teachers.
// It could be expanded later with teacher-specific assignment features like grading.
const TeacherAssignments = () => {
    return <Assignments />;
};

export default TeacherAssignments;
