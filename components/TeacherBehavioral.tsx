import React from 'react';
import BehavioralRemarks from './BehavioralRemarks';

// This component can be a wrapper for now, providing a specific entry point for teachers.
const TeacherBehavioral = () => {
    return <BehavioralRemarks />;
};

export default TeacherBehavioral;
